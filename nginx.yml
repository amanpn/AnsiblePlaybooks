---
- hosts: webserver
  become: true
  tasks:
    - name: Install Nginx Server
      yum: name=apache2 update_cache=yes state=present
      notify:
        - Start nginx Server
  handlers:
    - name: Start nginx Server
      service:
          name=apache2
          state=restarted
...
